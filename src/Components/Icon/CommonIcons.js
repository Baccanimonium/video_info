import Icon from "@/Components/Icon/index"
import { close } from "@/Icons/close"
import { toggleIndicator } from "@/Icons/toggleIndicator"
import { dragIndicator } from "@/Icons/dragIndicator"
import { removeIcon } from "@/Icons/removeIcon"
import { apply } from "@/Icons/apply"
import { editIcon } from "@/Icons/editIcon"
import { basketTrash } from "@/Icons/basketTrash"

export const IconClose = Icon(close)
export const EditIcon = Icon(editIcon)
export const IconToggleIndicator = Icon(toggleIndicator)
export const IconDragIndicator = Icon(dragIndicator)
export const RemoveIcon = Icon(removeIcon)
export const ApplyIcon = Icon(apply)
export const TrashIcon = Icon(basketTrash)
